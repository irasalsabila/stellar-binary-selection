"""Top-level package shim.

This file lives inside src/ so the package is importable as
`stellar_binary_selection.*` without installing the project, by setting
`PYTHONPATH=src` for tests / scripts.
"""

from __future__ import annotations

__version__ = "0.1.0"